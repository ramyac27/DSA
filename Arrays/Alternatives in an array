class Solution {
    // function to print alternate elements of an array
    public ArrayList<Integer> getAlternates(int arr[]) {
        ArrayList<Integer> ans = new ArrayList<>(arr.length/2);
        for(int i=0;i<arr.length;i=i+2){
            ans.add(arr[i]);
        }
        return ans;
    
    }
}
